<Window x:Class="ViewModel.ListBinding.ListBindingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local1="clr-namespace:SsbHedger.Model"
        mc:Ignorable="d"
        Title="SSB Hedger" Height="450" Width="800" Closing="Window_Closing">
    <Window.DataContext>
        <local1:MainWindowViewModel></local1:MainWindowViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="200"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"></ColumnDefinition>
            <ColumnDefinition Width="30"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="10"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Label x:Name="label" Grid.Row="1" Grid.Column="1" Content="SSB Hedger" 
               HorizontalAlignment="Left" Height="33"  
               VerticalAlignment="Top" Width="141" FontSize="16" 
               FontWeight="Bold" Grid.ColumnSpan="2"/>

        <Image Grid.Row="2" Grid.Column="1" Width="30" Height="20" 
               Source="pack://application:,,,/SsbHedger;component/Images/GreenCircle.png" />

        <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding ConnectionMessage}"></TextBlock>
        
        <ListBox Name="messages" Grid.Row="4" Grid.Column="1" 
                 Background="Azure" ItemsSource="{Binding Messages}"
                 Grid.ColumnSpan="3">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ReqId}"></TextBlock>
                        <TextBlock Text=" "></TextBlock>
                        <TextBlock Text="{Binding Body}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
